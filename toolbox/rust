# rustup
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

# cargo update
cargo install cargo-update

# starship
sudo dnf -y install cmake && cargo install starship

# rust utils not in dnf (yet)
cargo install topgrade
cargo install bacon
cargo install grex
cargo install mdcat
cargo install tealdeer

